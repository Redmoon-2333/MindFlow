# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# 环境
.env
.venv/
venv/

# 数据与模型 (本地隐私数据不入库)
backend-next/data/*.db
backend-next/data/*.db-wal
backend-next/data/*.db-shm
backend-next/data/logs/
backend-next/data/models/
data/mindflow.db*
data/logs/
data/backups/

# 数据集走 Git LFS（.gitattributes 追踪，此处不忽略 — LFS 指针入库，大对象存 LFS）
# 注意：manictime/awt 的 *.csv 由 .gitattributes 显式 LFS 跟踪，
# 不要在这里 ignore，否则 git add 会被跳过。

# 前端
node_modules/
dist/

# 打包
build/
*.spec.bak

# IDE
.idea/
.vscode/

# OMC 运行时状态
.omc/
